# --- Node / PWA ---
node_modules/
dist/
build/
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Rust / WASM ---
# We ignore the 'target' folder because it's massive.
# BUT we make sure to keep the built WASM in public/
miner-client/target/
pkg/

# --- Hardhat / Smart Contracts ---
artifacts/
cache/
typechain-types/
coverage/
coverage.json
*.db
*.sqlite

# --- OS / Mobile ---
.DS_Store
.userland_history
.bash_history
thumbs.db


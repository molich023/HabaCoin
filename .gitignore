# --- Node / PWA ---
node_modules/
backend/node_modules/
dist/
build/
.env
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Rust / WASM ---
# We ignore the cargo target artifacts because they are massive.
# BUT we explicitly unignore the compiled browser-ready public targets.
miner-client/target/
pkg/
!public/wasm/

# --- Hardhat / Smart Contracts ---
artifacts/
cache/
typechain-types/
coverage/
coverage.json
*.db
*.sqlite

# --- OS / Mobile / Sandboxes ---
.DS_Store
.userland_history
.bash_history
thumbs.db


.env
secret.json
*.key
artifacts
cache
node_modules
